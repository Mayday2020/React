
import s from './ProfileInfo.module.css';

const ProfileInfo = () => {
    return (
        <div className={s.descriptionBlock}>
            <div>
                avatar + description
            </div>
        </div>
    )
}

export default ProfileInfo;